namespace AIStudio.Provider;

public enum ConfidenceLevel
{
    NONE = 0,
    UNTRUSTED = 1,
    
    UNKNOWN = 10,
    
    VERY_LOW = 90,
    LOW = 100,
    
    MODERATE = 150,
    
    MEDIUM = 200,
    
    HIGH = 300,
}